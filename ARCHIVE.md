# ARCHIVE

Deleted/archived items. Git history is the archive — recover any row with
`git show <sha>:<path>` or `git checkout <sha> -- <path>`.

| Path | ~LOC | Last SHA | Reason | Date |
|------|------|----------|--------|------|
| `crates/dash-glossary` | 202 | `bd2859f` | CI guard broken since the repo split: hardcodes the pre-split path `crates/tooling/ndn-dashboard`, so its check step could never pass here (P2) | 2026-08-21 |
