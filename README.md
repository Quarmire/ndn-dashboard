# ndn-dashboard

The NDN operator dashboard (Dioxus) for the ndn-rs ecosystem, plus the
`ndn-dashboard-next` variant and the `dash-glossary` CI guard.

Depends (via git+tag) on `ndn-rs` (core), `ndn-ext` (faces, dashboard-core,
onboarding) and `ndn-fwd` (CLI helpers in `ndn-tools-core`).
