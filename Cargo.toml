[workspace]
resolver = "2"
members = [
    "crates/ndn-dashboard",
    # Adopted from ndn-fwd (P1): wasm-only browser demo, UI tier like the rest
    # of this repo. Not a default member — build explicitly for wasm32.
    "crates/dioxus-demo",
]
default-members = [
    "crates/ndn-dashboard",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
authors = ["ndn-rs contributors"]
license = "MIT OR Apache-2.0"

[workspace.metadata.crane]
name = "ndn-dashboard"

# Single source of truth for lint policy — members opt in via `[lints]
# workspace = true` (dioxus-demo does; mirrors the ndn-fwd/ndn-rs standard).
[workspace.lints.rust]
unsafe_code = "deny"

[workspace.lints.clippy]
dbg_macro = "warn"
todo = "warn"
unimplemented = "warn"

[workspace.dependencies]
# NDN workspace crates — grouped by subdirectory layer
ndn-tlv       = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3", default-features = false }
ndn-packet    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3", default-features = false }
ndn-mgmt-wire = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-transport = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-runtime   = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-store     = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
# Faces + browser plumbing for dioxus-demo
ndn-face-webtransport-wasm = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-webrtc            = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-face-shared-worker     = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-pib-idb                = { git = "https://github.com/Quarmire/ndn-fwd", tag = "v0.1.0-alpha.3" }
ndn-face-webble       = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-engine    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-app       = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-ipc       = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-config    = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-mgmt     = { git = "https://github.com/Quarmire/ndn-rs", tag = "v0.1.0-alpha.3" }
ndn-coding    = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-ratelimit = { git = "https://github.com/Quarmire/ndn-ext", tag = "v0.1.0-alpha.3" }
ndn-tools-core = { git = "https://github.com/Quarmire/ndn-fwd", tag = "v0.1.0-alpha.3" }

# Core async runtime — individual crates enable only the features they need
tokio      = { version = "1", default-features = false }
tokio-util = { version = "0.7.10", default-features = false }

# Zero-copy byte buffers (no default features — crates opt in to bytes/std explicitly)
bytes = { version = "1", default-features = false }

# Structured logging
tracing = "0.1"

# Async combinators and stream/sink extension traits
futures = "0.3"
async-trait = "0.1"

# Error handling
anyhow    = "1"
thiserror = "2"

# Serialization
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = { version = "0.22", default-features = false, features = ["alloc"] }

# Cryptography
ed25519-dalek = "2"
sha2          = "0.11"

# Logging subscriber (binaries only)
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# ── Build profiles ─────────────────────────────────────────────────────────────

# console: build ndn-fwd with tokio-console task inspector support.
#
# Note: `profile-rustflags` is not yet stable in Cargo; set the required
# --cfg tokio_unstable via the environment instead:
#
#   RUSTFLAGS="--cfg tokio_unstable" cargo build -p ndn-fwd --features console --profile console
#
# The profile inherits dev settings; add debug symbols and disable
# inlining to keep tokio-console task names readable.
[profile.release]
strip = "symbols"
lto = "thin"
codegen-units = 1

[profile.console]
inherits = "dev"
debug = true
opt-level = 0
